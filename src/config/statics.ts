export enum EKeyboardTypes {
  DEFAULT = 'default',
  EMAIL = 'email-address',
  NUMBER = 'number-pad',
  NUMBER_EXTENDED = 'numbers-and-punctuation',
}

export enum EFormTypes {
  EMAIL = 'emailAddress',
  USERNAME = 'username',
  PASSWORD = 'password',
  NONE = 'none',
}

export enum EForms {
  USERNAME = 'username',
  PASSWORD = 'password',
}

export enum EFormLabels {
  email = 'forms.email',
  username = 'forms.username',
  password = 'forms.password',
}

export enum EFormPlaceholders {
  email = 'placeholders.email',
  username = 'placeholders.username',
  password = 'placeholders.password',
}
